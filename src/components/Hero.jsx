import React from 'react';
import Carousel from './Carousel';

const Hero = () => {
    return (
        <section className="h-screen">
            <Carousel />
        </section>
    );
};

export default Hero;
